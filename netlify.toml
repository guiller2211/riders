#############################################################################
# PERFORMIO

## The following Netlify configurration is for the Performio application ONLY
## In situations where multiple sites need to be deployed from a monorepo,
## Netlify configuration should be managed directly via the UI
#############################################################################

## -- BUILD -- ##

[build]
command = "npx nx build performio --skip-nx-cache && npx nx server:netlify performio --skip-nx-cache"
publish = "apps/performio/dist/public"

[build.environment]
PROJECT_NAME = "performio"

## - PROXY & CACHE -- ##

[[redirects]]
from = "/*"
status = 200
to = "/.netlify/functions/server"

# Favicon
[[headers]]
for = "/favicon.ico"
[headers.values]
"Cache-Control" = "public, max-age=3600"

# Images, Icons, etc.
[[headers]]
for = "/assets/*"
[headers.values]
"Cache-Control" = "public, max-age=3600"

# Remix
[[headers]]
for = "/build/*"
[headers.values]
"Cache-Control" = "public, max-age=60, s-maxage=60"

## -- FUNCTIONS -- ##

[functions]
directory = "apps/performio/dist/netlify"
external_node_modules = [
  "@remix-run/react",
  "@remix-run/node",
  "@supabase/supabase-js",
  "isbot",
  "remix-typedjson",
  "tslog",
  "react-prismazoom",
  "node-ts-cache",
  "node-ts-cache-storage-memory",
  "lodash",
  "@uniformdev/canvas",
  "@uniformdev/canvas-react",
  "@uniformdev/context-remix",
  "@reduxjs/toolkit",
  "formik",
  "ramda",
  "ramda-adjunct",
  "react-redux",
  "yup",
  "axios",
  "classnames",
]
included_files = ["apps/performio/dist/index.js", "apps/performio/data/**", "apps/performio/config/*.json"]
node_bundler = "esbuild"

## -- PLUGINS -- ##

#[[plugins]]
#package = "./tools/plugins/netlify/affected-build"
