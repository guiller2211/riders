import { View } from '../../components/atomic';
import { HeaderUtilityBarProps } from './HeaderUtilityBar.types';
import { HeaderLogo } from '../HeaderLogo';
import { HeaderAdditionalBar } from '../HeaderAdditionalBar';
import { HeaderUser } from '../HeaderUser';
import { HeaderCart } from '../HeaderCart';

const HeaderUtilityBar = (props: HeaderUtilityBarProps) => {
  const { navigation, logo } = props;

  return (
    <View>
     
    </View>
  );
}
export default HeaderUtilityBar;
