import type { ModalProps } from 'reshaped';

export const DEFAULT_SIZE: ModalProps['size'] = {
  s: 'auto',
  l: '30rem',
};

export const DEFAULT_POSITION: ModalProps['position'] = {
  s: 'bottom',
  l: 'end',
};
