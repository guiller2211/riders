import React from 'react';
import type { RadioProps } from 'reshaped';
import { Radio as ReshapedRadio } from 'reshaped';

const Radio = (props: RadioProps) => {
  return <ReshapedRadio {...props} />;
};
export default Radio;
