import React from 'react';
import type { DividerProps } from 'reshaped';
import { Divider as ReshapedDivider } from 'reshaped';

const Divider = (props: DividerProps) => {
  return <ReshapedDivider {...props} />;
};
export default Divider;
