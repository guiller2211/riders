import React from 'react';
import type { SwitchProps } from 'reshaped';
import { Switch as ReshapedSwitch } from 'reshaped';

const Switch = (props: SwitchProps) => {
  return <ReshapedSwitch {...props} />;
};
export default Switch;
