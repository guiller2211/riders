import React from 'react';
import { RadioGroup as ReshapedRadioGroup } from 'reshaped';

const RadioGroup = (props: any) => {
  return <ReshapedRadioGroup {...props} />;
};
export default RadioGroup;
