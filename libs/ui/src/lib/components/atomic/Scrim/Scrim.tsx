import React from 'react';
import type { ScrimProps } from 'reshaped';
import { Scrim as ReshapedScrim } from 'reshaped';

const Scrim = (props: ScrimProps) => {
  return <ReshapedScrim {...props} />;
};
export default Scrim;
