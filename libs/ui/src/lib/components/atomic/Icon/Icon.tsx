import React from 'react';
import type { IconProps } from 'reshaped';
import { Icon as ReshapedIcon } from 'reshaped';

const Icon = (props: IconProps) => {
  return <ReshapedIcon {...props} />;
};
export default Icon;
